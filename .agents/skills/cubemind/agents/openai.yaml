interface:
  display_name: "Cubemind"
  short_description: "Repo-specific patterns and workflows for cubemind"
  default_prompt: "Use the cubemind repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true